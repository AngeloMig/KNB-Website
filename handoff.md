# KNB Web Solution — Project Handoff

> Working doc for continuing this build across Claude / Codex / other sessions.
> **Rule: whenever you finish or change something meaningful, update the "Changelog" and "Open items" sections at the bottom.**

---

## 1. What this is
Marketing website for **KNB Web Solution** — a Philippines-based web design & development agency.
Services: **Shopify** store revamps & ecommerce, **WordPress/WooCommerce** sites, **Webflow** & custom business websites, maintenance/support retainers, and custom web apps.
Primary CTA everywhere: **"Start your project."** (Was "Get a free audit" — changed 2026-06-19 because "audit" assumes an existing site and excluded new-build customers. Button/nav labels = "Start your project"; the *offer* in prose = "free consultation" / "written proposal". "Audit" is now only kept where it's a real deliverable, e.g. the Ecommerce plan's "Store audit & customer-journey review".)

It is NOT a Shark Tank / creative-branding agency — the site started as a clone of the Tankfluence Webflow template, so any leftover placeholder copy should be replaced with real KNB content.

## 2. Tech & how to run
- **Pure static** HTML/CSS/JS — **no build step, no framework, no package manager.**
- Just open the file in a browser: `open index.html` (or use a static server: `python3 -m http.server 8000`).
- External deps (all via CDN, need internet):
  - **GSAP 3.12.5 + ScrollTrigger** (cloudflare CDN) — all scroll/hover animation.
  - **Satoshi** font via Fontshare (`api.fontshare.com`).
  - **simpleicons CDN** (`cdn.simpleicons.org/<slug>`) — brand logos (shopify, webflow, wordpress, woocommerce, figma, framer).
  - **picsum.photos** — placeholder project/testimonial images (seeded URLs).

## 3. File map
| File | Purpose |
|---|---|
| `index.html` | Home page (markup only — CSS/JS are external now). |
| `styles.css` | All home-page styles (extracted from index.html). |
| `main.js` | All home-page JS (extracted from index.html). Runs after GSAP CDN scripts. |
| `contact.html` | Contact page — **still has its own inline `<style>`/`<script>`** (not yet refactored). |
| `portfolio.html` | Portfolio overview — links to the 3 platform pages. Uses `work.css` + `work.js`. |
| `shopify.html` / `webflow.html` / `wordpress.html` | Per-platform work pages, each with its own project grid. Use `work.css` + `work.js`. |
| `case-study.html` | **Reusable case-study template** (Lumen example). Duplicate per real project. Uses `work.css` + `work.js`. Linked from the portfolio spotlight. |
| `work.css` / `work.js` | Shared styles/JS for portfolio + platform pages (nav, footer, buttons, reveal). |
| `header.js` | **Global nav** — injected on every page via `<script src="header.js">`; derives active state + logo href from the URL. Edit the nav here. |
| `footer.js` | **Global footer** — injected on every page via `<script src="footer.js">`; base footer is identical everywhere, optional CTA band (homepage review CTA + per-platform CTAs) picked from an internal `CTAS` map keyed by page. Edit the footer here. |
| `headline-preview.html` | **Scratch/preview page** — used to compare design options (headline animations, marquee, stats counters, chevron behaviors, "how we work" ideas). Not linked from the site; safe to ignore or delete. |
| `hero-card-alternatives-preview.html` | **Scratch/preview page** — 5 alternative homepage hero platform-card interactions (physical edge flip, split-door reveal, layered blueprint stack, command cube, peel-back brief) + a copy-paste prompt. Not linked from the site. |
| `pricing-section-alternatives-preview.html` | **Scratch/preview page** — 5 alternative layouts for the pricing/package selector section ("Pick a starting point"): command selector, comparison matrix, package journey, scope builder, proposal desk. Not linked from the site. |
| `index.html.bak` | Backup taken before the CSS/JS extraction refactor. Delete when confident. |
| `handoff.md` | This file. |

> Note: there are **two CSS/JS systems** — home page uses `styles.css`/`main.js`; the portfolio/platform pages use `work.css`/`work.js`; `contact.html` uses `contact.css`/`contact.js`. Footer **CSS** is still duplicated across `styles.css` + `work.css` + `contact.css` (sync all three). But footer **markup** is now single-source in `footer.js` (like the nav in `header.js`) — edit footer content there, not in the pages.

## 4. Conventions & hard rules
- **NO PRICING on the public site.** KNB's pricing, commission %, and internal procedures are confidential (per their SOP). Plans/packages show *coverage/scope only* + "Get a quote / free audit". Client-facing "Starting at …" prices exist in their guide but are **not added yet** — only add if the user explicitly asks.
- **No sales/ranking guarantees** in copy (their SOP forbids it).
- Email placeholder is `youremail@gmail.com` (not yet replaced with a real address).
- Brand: off-white bg `#f4f3ef`, near-black ink `#111110`, pink accent `#e83fa0`, green status dot `#6cf09a`. Font weights are light (Satoshi 500 for headings, not 800).
- Animations are GSAP-driven; most use `ScrollTrigger`. Respect `prefers-reduced-motion` where added.
- The site is centered/responsive; `html, body { overflow-x: clip }` prevents horizontal scroll — don't reintroduce wide/un-clipped elements.

## 5. Home page (`index.html`) section rundown
Order: **nav → hero → marquee → stats → services → process → projects → solutions (plans + care toggle) → why-knb → logo wall → testimonials → faq → footer.**

- **Nav** — floating glass pill; transparent over hero, solid blur on scroll; mobile hamburger → full-screen overlay; CTA "Free audit".
- **Hero** — caption "Design · Build · Convert"; headline "KNB Web Solution" with **cursor-reactive letters** (lift toward mouse); 3 platform cards each carrying a green-dot **slot-availability pill** (`.plat-slot`, on the card front above the name — Webflow "2 slots available", Shopify & WordPress "1 slot available"; update these by hand as bookings fill) (Shopify center / Webflow left / WordPress right) that **deal in (spread outward), float + tilt, gradient-drift + sheen**, and on **hover grow + raise z-index above the headline** (no fan/compress). Background: drifting grid + cursor spotlight + canvas dust particles (pink-tinted). Foot: subtitle left, CTA + email right.
- **Marquee** — two opposing rows; hover pauses a row + highlights the hovered word pink; ✸ separators are pink.
- **Stats** — 3+/40+/100+ count up on scroll.
- **Services** — scroll-fill intro (grey→black word by word) on the left + **floating tech-logo cluster** (Shopify/WordPress/Webflow/Figma/Framer/WooCommerce badges, 134px, compact honeycomb) on the right; then 01–04 service rows.
- **Process ("How we work")** — heading + 4 steps that **flow in left→right on scroll**; big drifting **"PROCESS"** watermark; **full-width chevron "comet"** band at the top of the steps (1 black + ~5–6 fading tail + rest invisible, flows left→right infinitely, JS-generated to fill width).
- **Projects ("Recent websites & store revamps")** — header + **"View full portfolio"** button; **one-row filter bar** — platform pills (All/Shopify/Webflow/WordPress) · divider · goal pills (Online store / Business site / Editable CMS), with a live **"X of 5 highlights shown"** status + empty state (the old stat band + "Platform"/"What you need" labels were removed per design feedback 2026-06-20); **5 cards** (1 featured) in browser frames showing **real screenshots via WordPress mShots** (shimmer skeleton + blur-up + picsum fallback); each card has a clickable platform chip (filters + sets `#work=` hash), category + industry chips, a "View details" button + clickable screenshot → **detail modal** (large shot + Screenshot/Live-preview iframe toggle + scope + tags + live link + optional case-study link). Cursor dot-spotlight background. JS lives in `main.js` (filter / count-ups / screenshots / modal IIFEs).
- **Solutions ("Solutions for every stage", `#plans`)** — ONE section with a **Website projects / Care plans toggle** (`.solu-toggle`). Each panel is a clean **card list** (`.sol-list`/`.sol-item`: icon tile + name + tagline + arrow, "Popular" badge) — **same design transferred from the pricing page selector** (no inline detail panel — per request). Website: Starter/Business(Popular)/Ecommerce/Custom Web App. Care: Basic/Standard/Growth(Popular)/Premium/Custom System. **Every card + the foot button link to `pricing.html`** (where the full per-package/plan details live) — foot button reads **"See full details →"** (deliberately NOT price wording; homepage shows no prices). (Merged from the old Plans + Maintenance sections 2026-06-21.) Background: pink glow. *Dead now: `.pkg*`/`.pp-*`/`.mrow*`/`.maint-alt*` CSS + the `.pkg-item` selector JS + `.maint` ScrollTrigger — all unused.*
- **Why KNB** — 4 value points with icons that spin in; scroll-reactive pink rail + "04" marker.
- **Logo wall** — second marquee of placeholder client logos.
- **Testimonials** — slider, arrows, 5s autoplay; star-pattern bg.
- **FAQ** — accordion; "FAQ" watermark.
- **Footer** — logo, socials (SVG), copyright.

Each section has a **distinct scroll-in animation** (slide-left, scale-pop, 3D flip, slide-right, rise+icon-spin, etc.) defined in `main.js`.

## 5b. Hero narrative bubbles (comic/manga design) — ✅ backlog done 2026-06-20
The hero platform cards (Shopify/Webflow/WordPress) have **comic speech bubbles**. Files: markup inline in `index.html` (inside each `.card pX`, first children before `.polaroid`), styles `.nb` / `.nb-in` / `.nb-burst` / `.tail-*` in `styles.css`. Hover-reveal only (`.card:hover .nb`), CSS-driven stagger.
**Current state:** **4 per card** on the left/right cards, **3** on the center card (its sides are too tight for outer bubbles). Hidden **under 1200px** (raised from 980 so side bubbles never clip on smaller desktops). Reveal staggered in reading order; alternating initial rotation; springy pop-in + idle wobble.

### Improvement backlog — all 5 DONE (2026-06-20)
1. ✅ **Collision-proof positioning.** Bubbles moved off the headline band and pulled in from the viewport edges; bursts now sit on the card-top (below the headline), side bubbles straddle the outer card edge (≤~90px out), rest on top/bottom. Verified at 1280. Center card uses top+bottom only.
2. ✅ **Burst lead bubble.** First bubble per card is `.nb-burst` — spiky `clip-path` starburst, pink fill, hard `drop-shadow` offset, no tail. Cards bumped to 4 (center 3).
3. ✅ **Comic texture.** Faint halftone dot pattern inside the white bubbles, uneven hand-drawn `border-radius` (alternating per `:nth-child`), and "speed lines" (`.nb-burst::before` repeating-conic-gradient) that flick out on hover.
4. ✅ **Sequencing + motion.** Reading-order reveal via per-`:nth-child` transition-delays (1→4), alternating initial rotate for varied settle, overshoot easing retained.
5. ✅ **Content strategy.** Mixed copy: benefit / proof / verb-voice, ≤3 words (e.g. Shopify: "Built to sell" burst, "Secure checkout", "Conversion ↑").

Still optional / not done: a **tap-to-reveal** fallback for touch (bubbles are hover-only, hidden <1200px, so mobile/tablet users never see them).

## 6. Open items / TODO
- [ ] **Booking / "Book a call" feature** (deferred 2026-06-23) — let visitors schedule a consultation. Recommended: embed **Calendly or Cal.com** (free tier) as a popup from the existing "Book a free consultation" / "Start your project" CTAs, OR inline on contact, OR a `/book` page. Needs the client's scheduler link. (Custom date/time form via Netlify is the no-external-tool fallback, but it's a request not a live booking.)
- [x] ~~Replace `youremail@gmail.com` with the real email everywhere.~~ → now `info@knb.solutions` (2026-06-22). Confirm this is the final inbox.
- [ ] **Home testimonials** now show **invented client names on real KNB project brands** + placeholder quotes and a placeholder "4.9" rating — get **written permission + real quotes/rating** before launch. Do NOT ship fabricated attributed quotes.
- [ ] `contact.html` still has inline CSS/JS — could refactor to share `work.css`/`work.js` or `styles.css`.
- [ ] Project cards, testimonials, logo wall still use **placeholder** images (picsum) and **made-up** project names + scope lines — swap for real work when available. (When real assets exist, the natural next step is per-project case-study pages so cards can link to detail again.)
- [ ] Decide whether to add real client-facing prices to Plans (currently intentionally omitted).
- [ ] `index.html.bak` backup can be deleted once the refactor is confirmed stable.
- [ ] Footer/socials point to `#` — add real social links.
- [ ] Platform work pages (shopify/webflow/wordpress) use placeholder projects too.
- [ ] **Portfolio testimonials are placeholder** (`[Client name]` / `[Sample Brand]`) — replace with real, attributable quotes (with permission) before launch. Do NOT ship fabricated quotes.
- [ ] **Portfolio marquee** shows placeholder brand wordmarks — replace with real client logos or remove before launch.
- [ ] **Portfolio stats** ("18 / 3 / 12+") are placeholder — confirm the real numbers before launch.
- [ ] Add `og:image` + absolute `og:url` to `portfolio.html` (and ideally all pages) once hosted on the real domain. SEO meta only added to `portfolio.html` so far — other pages still need description/OG tags.
- [ ] `case-study.html` is a **template** — duplicate it per real project and link the matching project cards to each, once real case-study content exists.

## 7. Changelog (append newest at top — keep this updated!)
- **2026-07-04 (services revamp, batch 2 — image-forward)** — Added `services-revamp-alternatives-2-preview.html`: 5 more "What we help with" revamps, all image-led, per user "make it creative + add image placeholder". Shared `SVC` array (same 6 services); real mshots for Shopify/Webflow/WordPress, a clearly-labelled `.ph` "Image placeholder" block (accent gradient + icon + caption) for revamps/custom/care. F **Image-top gallery** (screenshot on top of each card); G **Polaroid wall** (tilted photo frames, straighten on hover); H **Showcase + thumbnails** (big product shot swapped by a 6-thumb rail, hover/click); I **Magazine spread** (zig-zag feature rows, big image alternating sides); J **Overlay hover cards** (full-bleed image tiles, hover slides up desc/tags/proof/CTA). Verified: 6 items + exactly 3 placeholders per grid, H stage swaps on thumb click, console clean, no mobile h-overflow at 375px. Not linked. (Batch 1 non-image directions A–E in `services-revamp-alternatives-preview.html`.)
- **2026-07-04 (services bento revamp preview)** — Added `services-revamp-alternatives-preview.html`: 5 revamps of the homepage "What we help with" capability bento (`.services`/`.v3-*` in index.html, 6 services). All render from one shared `SVC` data array (identical copy: Shopify/Webflow/WordPress/Revamps/Custom apps/Care, with best-for, tags, proof, per-service `--c`, browser mocks) and reuse homepage/package tokens (radii, --shadow-md, pills, tags). Directions: A **Editorial index** (numbered scannable rows, accent tint on hover); B **Tab switcher** (segmented control → one detail panel w/ mock + CTA, matches pricing tabs/fit-check); C **Refined bento** (feature card 2×2 w/ mock, ghost initial watermarks, unified hover, pinned proof — tidies the current one); D **Ink split** (dark feature panel + accent-icon rail, ties to quiz/care/fit-check ink); E **Filter by need** (need chips highlight the matching card, like portfolio filters + picker matchmaker). Verified: all 6 items render in each, B panel swaps on tab click, E highlights the hit card, console clean, no mobile h-overflow at 375px. Not linked from the site.
- **2026-07-04 (pricing CTA alternatives, batch 3 — all rip-on-hover)** — Added `cta-quote-alternatives-3-preview.html`: 5 more ticket CTAs, all tearing on hover like the shipped `.ctaC`, each ripping a different direction: K boarding-pass stub flies RIGHT (3D peel + barcode), L cinema bottom-bar tears DOWN (rotateX), M perforated ticket splits APART (both halves separate), N coupon stub tears LEFT (mirror), O receipt top-strip peels UP. Shared `.rc`/`.keep`/`.tear` scaffolding + reusable jagged clip-path vars (--jag-l/r/t/b); rip driven by a JS `is-ripped` class on pointerenter/focus (reduced-motion-safe + testable). Verified: all 5 transforms+clips fire (measured rest vs ripped), K ripped-state screenshot confirms torn jagged edges + gap, console clean, no mobile h-overflow at 375px. Not linked. Three CTA preview batches now exist: `-preview` (non-ticket A–E), `-2-preview` (static ticket F–J), `-3-preview` (rip-on-hover K–O).
- **2026-07-04 (pricing CTA alternatives, batch 2 — ticket-inspired)** — Added `cta-quote-alternatives-2-preview.html`: 5 more directions for the "Ready for an exact quote?" CTA, all in the ticket/coupon family (user wanted ticket-inspired variety, since the shipped `.ctaC` is a rip-on-hover coupon). F **Boarding pass** (FROM→TO route strip + barcode stub + seam notches); G **Cinema stub** (dark ticket, pink vertical ADMIT ONE rail, serial no.); H **Cut-out coupon** (dashed cut border, scissors mark, FREE·QUOTE redeem code chip, scalloped edges); I **Luggage tag** (clip-path chevron edge + punched hole + string, tilts, straightens on hover); J **Raffle stub** (numbered main + tear-off "your half" with vertical No. 006, perforated seam + notches). Same copy as batch 1. Verified headless: all 5 render, console clean, no mobile h-overflow at 375px. Not linked from the site. (Batch 1 non-ticket directions are in `cta-quote-alternatives-preview.html`.)
- **2026-07-04 (pricing CTA alternatives preview)** — Added `cta-quote-alternatives-preview.html`: 5 directions for the pricing-page "Ready for an exact quote?" CTA (currently `.cta-strip`/`.ctaC` — the ticket/coupon that rips on hover). Same copy in all: A **Ink statement slab** (dark band + pink glow, most on-brand, matches footer CTA/care flagship/terminal); B **Proposal preview** (mock document making "written proposal" literal — scope/timeline rows + price line kept as "sent in writing" per no-public-pricing rule); C **Process rail** (3 steps → CTA as the final node, reuses the pricing process language); D **Paste-your-URL starter** (single field echoing the contact free-review; carries the URL to `contact.html?site=`); E **Editorial big-type** (no card, huge headline with the ask as an oversized underlined link). Verified headless-preview: all 5 render, console clean, D form builds `contact.html?site=…` and navigates, no mobile h-overflow at 375px. Not linked from the site. NOTE: `--fc-pass`/fit-check etc. untouched. My pick: A (safest, most cohesive) or D (lowest friction).
- **2026-07-04 (global footer — one footer for all pages)** — Mirrored the `header.js` pattern for the footer. Created **`footer.js`**: injects the base footer (byte-identical brand block + 3 nav columns + KNB watermark + copyright/back-to-top) at the `<script src="footer.js">` position, and picks an optional CTA band from an internal `CTAS` map keyed by page (`index` = 2-col review CTA; `shopify`/`webflow`/`wordpress` = single-headline platform CTA; all others none). CTA `em` colour comes from each page's own `--pcol`, so platform CTAs theme automatically (verified Shopify green #5e8e3e). Replaced the inline `<footer>…</footer>` on all 10 pages with the one-line script; **404.html gained a footer for the first time**. Footer CSS unchanged (still in styles.css/work.css/contact.css — sync those). Verified live: footer injects + renders on index/shopify/contact/privacy/404, homepage review CTA intact, platform CTA themed, back-to-top handler still wired (work.js runs after injection — click scrolls to 0), no console errors, mobile 375px stacks with no h-overflow. **Now edit footer content in ONE place (`footer.js`).**
- **2026-07-04 (fit-check → picker bridge + portfolio deep links)** — The page's two interactive sections now talk: each platform's fit-check JSON gained a `"goto"` hint on its wrong-tool mode (shopify content→webflow, webflow store→shopify, wordpress store→shopify); the runner dispatches `knb:fitcheck` on every run and a new bridge IIFE in work.js pre-opens the suggested picker column — `.pp-suggest` + `.pp-cols.has-suggest` (CSS mirrors the expanded/hover states; the current column yields the floor; hovering elsewhere yields the suggested one too), with the tag swapped to "**The check pointed here**" (original restored when the answer changes back). Also: the current column's dead "You're on this page" text is now a live link into the filtered portfolio ("See all 6/10/28 X builds →" → `portfolio.html#work=<platform>`, which the portfolio's existing hash-filter handles). Verified via harness on shopify: portfolio href correct, suggest applied + tag swapped on "Mostly content", cleared + tag restored on switch-back, and layout swap confirmed with transitions neutralized (suggested 636px / cur 273px). Mobile unaffected (all columns open there; suggest classes are inert under 820px).
- **2026-07-04 (picker collapsed-state revamp, + bigger spine type + baseline alignment per user)** — Follow-ups: spine name bumped 17px → clamp(22px,2.2vw,30px), caption 10.5→12px w/ .16em tracking, watermark 92→108px at top:30%; then name+caption bottom-aligned as one lockup (writing-mode moved from the flex container onto the b/small children, container `align-items:flex-end; flex-direction:row-reverse` — container-level writing-mode staggered the two lines). — User disliked the closed columns (read as white voids even with spines). Collapsed panels are now designed objects: (1) **platform-tinted wash** (`--ppbg` gradient from 8% accent to white; expanded panels go solid white for legibility, the current column re-tints when it yields the floor); (2) **large centered watermark initial** (`.pp-mark`, 92px at 15% accent, crossfades out on expand); (3) **drawer peek** — `.pp-shot` no longer hides when collapsed but sits at `translateY(calc(100% - 40px))`, so the featured build's browser bar (feedbird.com / coptrz.com / lightmybricks.com) peeks from the bottom edge and slides up on expand; spine moved up (bottom:56px, z-index above the peek). Mobile: marks hidden, white bg, static shots (verified 390px, no overflow). Reduced-motion covered. Console clean.
- **2026-07-04 (design-review tweaks on the two shipped sections)** — Post-review polish: (1) **Picker collapsed columns furnished** — the vertical spine is now name + a small uppercase "best for" caption in the platform accent (`.pp-vname b/small`; captions: "selling online" / "design-led sites" / "content & woo"), and section min-height trimmed 430→390px, killing the empty-slab look. (2) **Fit-check terminal now takes each page's accent**: new `--fc-pass` (lightened `--pcol` via color-mix for ink contrast) drives the `$` prompt, ✓ pass lines, verdict keyword and cursor — pastel green on Shopify, periwinkle on Webflow, steel blue on WordPress; amber warns stay universal. Verified via section screenshots on shopify (spines) + webflow (blue terminal). Third review nit (extra dark space on 3-line terminal runs) intentionally left — constant height avoids layout jump when switching modes.
- **2026-07-04 (SHIPPED: accordion platform picker on all platform pages)** — Replaced the `.pc-card` "Pick the right platform" grid on shopify/webflow/wordpress with the **accordion-columns direction (picker Direction A), elevated per user's "add more creativity"**: `.pp-cols` flex columns where the current platform holds the wide panel (platform-color ring + "You're here" pill) and hovering/keyboard-focusing another column hands it the floor (`:has()`; graceful without it — two wide columns). Creative additions over the preview: per-platform **identity rail** (4px top bar), **rotated vertical platform names** on collapsed columns, **ghost initial watermark** (S/W/WP) in the open panel, and the payoff — each platform's **featured build** (Light My Bricks / Feedbird / Coptrz mshots in a mini browser frame) rises from the open panel's bottom edge. CSS-only interaction, no JS; `aria-hidden` on decorative bits; reduced-motion = no transitions; ≤820px stacks all-open with vertical names hidden and static shots. Removed old `.pc-grid/.pc-card` base + enhancement CSS blocks (incl. `pcPulse`), the stagger selector refs, and the dead pop-in IIFE in work.js (`.pc-head` styles kept — still used). Verified: div/span balance, CSS braces, zero console errors, structure harness on webflow (cur widest 636px vs 273px, correct links, shot visible, vname hidden), section screenshot, 390px stack with no overflow.
- **2026-07-04 (SHIPPED: Direction O fit-check console on all platform pages)** — Replaced the `.why-plat` "is this right for you?" section on shopify/webflow/wordpress with the **live fit-check terminal** (batch-3 Direction O, user's pick). New `.fitcheck` section (keeps `id="overview"` + the platform-tinted top gradient): left = eyebrow/h2/lead + `.fc-seg` selector (aria-pressed) + `.fc-proof` stats + "Check says no?" cross-links; right = ink terminal (`knb — fit check`) that prints per-mode check lines with dotted leaders then the verdict, first run triggered by IntersectionObserver when scrolled into view (6s safety fallback; reduced-motion = instant). Per-page copy/checks live in a `<script type="application/json" data-fc-data>` inside each section — edit content there, no JS changes needed. Platform configs: Shopify (store/content/both, GREAT FIT / WRONG TOOL / STRONG FIT), Webflow (site/store/both — store honestly redirects to Shopify), WordPress (content/store/both — pure store verdict is "DEPENDS — Woo works; Shopify is simpler"). Shared styles in work.css (`.fc-*`), runner in work.js. REMOVED: old `.why-*` markup on all 3 pages (incl. the why-side featured shot + "other options" card — featured stores still appear in each page's project grid), the ~70-line why CSS block + orphaned `.why-best` rules, and the why-plat glow/fit-toggle IIFE in work.js. Verified: div balance, JS/CSS parse, zero console errors on all 3 pages, mode switching + aria state on all 3 (incl. per-page verdicts), section screenshot matches the preview, no horizontal overflow at 390px (terminal 319px, one column, lines wrap). Preview files kept for reference.
- **2026-07-04 (fit-check skins preview — user favors Direction O)** — User loves the batch-3 Direction O "system check" for the fit section but asked whether the terminal aligns with the design + for 5 restyles. Added `fit-check-variants-preview.html`: same live-check mechanic in five skins, ordered most→least on-brand (O1 **Paper fit report** — Satoshi/off-white/tinted pills, zero monospace, the safest ship; O2 **Browser window** — the site's own bw traffic-light motif with a DevTools-style drawer; O3 **Status board** — ink mission-control tiles with LEDs + verdict lamp, matches quiz/care ink panels; O4 **Inspection sheet** — clipboard with tick boxes + rotated rubber stamp; O5 **CI pipeline** — nodes light along a line, release badge verdict, vertical on mobile). Shared data + per-skin renderers, sequential reveal, reduced-motion instant. Verified headless: clean render, console clean, all five re-run correctly per mode via harness. Alignment note: the original terminal's window chrome + ink panel are existing motifs; monospace type is the only new ingredient (O1/O3/O4 avoid it entirely, O2 confines it to the drawer).
- **2026-07-04 (platform-picker alternatives, batch 2)** — Added `platform-picker-alternatives-2-preview.html`: five more directions (F–J) for the "Pick the right platform" section, deliberately object-metaphor heavy per user's "be creative" (F **Pegboard** — line-art tools on a dotted pegboard, current one askew "in your hand"; G **The dock** — platforms as running apps, dot under the active one, magnify + tooltip on hover; H **Fan deck** — Pantone-style swatch fan, current chip pulled out, deck spreads on hover; I **Signpost** — crossroads arms clip-pathed as arrows, current arm ink-filled with green dot, stacks plainly on mobile; J **Radio tuner** — dark dial with a pink needle idling on the current station, hover swings it + previews, click retunes/navigates). Verified headless: clean render at 1280, console clean, tuner logic exercised (needle move, readout, retune link show/hide, home reset). Not linked from the site.
- **2026-07-04 (platform-picker alternatives preview)** — Added `platform-picker-alternatives-preview.html`: 5 directions for the "Pick the right platform" cross-link section at the bottom of the platform pages (A **Accordion columns** — current platform holds the wide panel, hover/focus hands the floor to another; B **Needs matrix** — compare by need-rows with one honest "Best fit" chip per row, current column tinted; C **Matchmaker** — "what matters most?" chips light the matching platform, choosing the current one says "you're already in the right place" instead of a circular link; D **Hairline switcher** — one ruled band, reclaims the whole section's height; E **Boarding passes** — ticket stubs with perforations, current one stamped "You're here", matches the homepage ticket CTA language). Shown in Shopify-page state; per-platform accents via simpleicons. Verified headless: clean render at 1280, console clean, matchmaker logic exercised (hit switching, current-platform "stay" state, toggle-off). Not linked from the site.
- **2026-07-04 (platform-fit alternatives, batch 3)** — Added `platform-fit-alternatives-3-preview.html`: five more directions (K–O) for the "When Shopify is the right call" section (K **Before/after** — draggable comparison of "your week self-managed vs on Shopify", same slider language as the portfolio case study, range-input based so it's keyboard accessible; L **Signal checklist** — visitor ticks what's true, fit meter + verdict accumulate honestly incl. counter-signals; M **Case echo** — Light My Bricks carries the pitch with scope facts only, NO invented quotes, then "your turn" selector; N **Transit map** — platforms as transit lines from one origin, picked line lights up, others dim toward their pages; O **System check** — live terminal that re-runs the fit check per answer with staggered line reveal, reduced-motion falls back to instant). Verified headless: renders clean at 1280, console clean, all four interactive mechanisms exercised via iframe harness (slider drag, checklist math, line switch, terminal re-run). Not linked from the site.
- **2026-07-04 (platform hero markup fix)** — The 07-03 stats swap left an **extra `</div>`** after the new `.pf-stats` block on shopify/webflow/wordpress, closing the hero wrapper early and pushing `.hero-mock` out of the two-column split (mock rendered full-width). Removed the stray close on all three pages; div counts now balance (147/171/279 open=close) and headless renders confirm the proper copy-left / mock-right hero with per-platform accent dots. Console clean.
- **2026-07-04 (platform-fit alternatives, batch 2)** — Added `platform-fit-alternatives-2-preview.html`: five NEW directions (F–J) for the "When Shopify is the right call" section, requested after the preview-file cleanup removed batch 1 (F **Fit scorecard** — quantified bars that re-weigh per answer, honestly mediocre for content-led; G **Fork in the road** — three route panels, picked path lifts in platform green, others dim toward Webflow/WordPress; H **Till receipt** — monospace fit-check receipt with dotted leaders, scalloped tear, VERDICT total row; I **Editorial verdicts** — no cards, headline-sized self-answering rows with expanding detail; J **Admin mock** — Shopify-admin-style "storefront health" panel that greys out for content-led). One delegated JS handler drives all selectors via `data-mode`/`data-verdict`; reduced-motion respected. Verified headless: renders clean at 1280, console clean, and mode-switching exercised via iframe harness (bars/grade, receipt lines/total, row expand, overlay). NOTE: working tree currently has ALL older `*-preview.html` files staged as deleted (someone's cleanup) — this new file is intentionally additive; don't resurrect the deleted ones.
- **2026-07-03 (hero stats direction reversed + Packages menu overview link)** — Per user: the earlier stats unification went the wrong way. Reverted `portfolio.html` to its original big stacked `.pf-stats` numbers and applied THAT design to shopify/webflow/wordpress heroes instead (6/10/28 + 40+ + 12+, `data-count` count-up; the label dot now uses `var(--pcol)` so each platform gets its accent — green/blue/wp-blue, pink on portfolio). Removed the now-dead `.hero-stats`/`.hs` CSS and the `data-num` counter IIFE from work.css/work.js. The portfolio `plat-mq` client marquee band from earlier stays. Also: the nav **Packages mega menu had no link to the pricing page itself** (every item deep-links to #build-*/#care/#quiz, so visitors never saw the pricing hero) — added a full-width `nm-foot np-foot` row: "4 packages · 5 care plans · quoted in writing / **See all packages & plans →**" linking to plain `pricing.html` (mirrors the Work menu's portfolio foot link). Verified headless: all four heroes render the stacked stats with correct accents, console clean, menu foot link present in injected nav.
- **2026-07-03 (portfolio hero/marquee unified + platform-fit alternatives preview)** — (1) `portfolio.html` hero stats now use the platform pages' design: `.pf-stats`/`.pf-stat` (big stacked numbers) replaced with `.hero-stats`/`.hs` (inline bold number + label, `data-num` count-up). (2) The portfolio client marquee now uses the platform pages' `.plat-mq` band design (thin bordered strip, dot separators, pink fallback accent) instead of the old `.logos` wordmark scroller — client names kept, permission TODO comment kept. Dead CSS removed from `work.css` (`.pf-stats*`, `.logos*`, `logoscroll` keyframes) and the dead `.logos-track` speed-up block removed from `work.js`; the off-screen animation pauser now targets `.plat-mq-track`. Verified headless: hero + marquee render, console clean. (3) Added `platform-fit-alternatives-preview.html` — 5 directions for the "When Shopify is the right call" section (A decision-first stage · B honest spec ledger · C ink assessment with pressed-in segment · D annotated storefront proof · E support-chat verdict, matching the pricing care chat language). All fit selectors are live (delegated JS, verdict + tone swap verified via iframe harness). Not linked from the site.
- **2026-07-03 (work pages: consistency/code/perf pass)** — Follow-up to the portfolio+platform review, content items deliberately skipped. **Consistency:** shopify.html CTA-strip label "Free review" → "Free consultation" (matches webflow/wordpress). **Code quality (work.js):** nav scroll listener now `{passive:true}` (was the only non-passive one); new IIFE pauses continuously-animating CSS decor (`.logos-track` logo marquee, `.work-wm` watermark drift) via IntersectionObserver while off-screen. **Performance:** testimonials.js marquee rAF loop is now IO-gated on `#testiWall` (rootMargin 200px) — it previously mutated `transform` every frame for the entire visit on every page that loads testimonials (portfolio, platform pages, pricing); portfolio's before/after spotlight images now `loading="lazy" decoding="async"`; added `preconnect images.unsplash.com` to shopify/webflow/wordpress (each loads 4 service photos). Verified headless (console-clean, full-page renders intact on all 4 pages; iframe harness confirms the marquee freezes off-screen — IO re-entry not testable under virtual time, logic is a standard gate). NOTE: agent-review false positives, do not "fix": hero "40+/12+" stats are the deliberate rounding convention; footer `href="#top"` works (spec fragment + work.js `.foot-up` handler).
- **2026-07-03 (extras/good-to-know CSS restored — same clobber, part 2)** — The same `pricing.html` clobber had also eaten the **"optional extras + good to know" base styles** (`.extras-row/.extras-card/.ex-*/.gtk-*`, HEAD lines 102–138). Symptom: the extras section rendered its SVG icons at full container width — giant black shapes adding thousands of pixels of "large space" mid-page. Restored from HEAD `3b5c070`; verified via headless-Chrome full-page screenshots at 1280 and 500 that extras/gtk render compact, the quiz section is dark, and no blank bands remain. A selector-level HEAD-vs-working diff now shows only intentional deletions (old `.cp-hrs`/`.cp-badge` care styles superseded by Direction C, dead `.chat-composer`). If more oddities surface, run that diff first — the clobber may have left other holes.
- **2026-07-03 (quiz CSS restored after file clobber)** — The working `pricing.html` had lost a ~100-line inline-CSS span (`/* quiz */` block, quiz result styles, `.pr-sticky` mobile quote bar, howit/extras media queries) — most likely a dying write from the maxed-out terminal session; the quiz section rendered completely unstyled (light bg, default buttons) and the mobile quote bar stopped being fixed. Restored the span from git HEAD (`3b5c070`, which already contained the July 3 quiz a11y/mobile additions). Deliberately NOT restored (markup no longer exists): `.care-cta`, `.care-info`/`.care-note`, `.cmp` hover rules, `.cmp-cue`. Verified: quiz dark section + cards + progress render, quiz JS works (advance/persist), quote bar fixed again, braces balanced.
- **2026-07-03 (care chat → corner launcher on tablet/mobile)** — On ≤900px the pricing care-plans chat box no longer stacks below the cards (it was eating a full screen of vertical space). It's hidden behind a **floating chat bubble** (`.chat-fab`, 56px, ink with off-white ring + green notification dot) that sticks to the section's bottom-right while scrolling the plans; tapping it pops the chat up as a **fixed bottom-right window** (370px max, `chatPop` animation, ✕ in the chat head, Escape closes, focus moves in/out). At ≤760px both sit above the fixed quote bar (fab `bottom:84px`, popup `bottom:152px`). Desktop unchanged (sticky 35% aside). JS: small toggle IIFE (`careChatFab`/`careChatBox`/`careChatClose`) in `pricing.html`. Verified at 768/375: launcher sticks, popup opens in-corner and clears the quote bar, Escape/close return focus, no horizontal overflow.
- **2026-07-03 (care plan cards — Direction C shipped)** — Shipped `plan-card-alternatives-preview.html` **Direction C "Ink flagship"** into the `pricing.html` care plans panel, per user choice, with one tweak from Direction E: the hour figure is now a **pressed-in inset pill** (`.cp-hpill`, same "caved-in" language as the tab switcher). Growth goes full ink with green accents (`#6cf09a` rollover line, green checks, translucent response chip); all cards get a faint hour watermark (`.cp-wm`); the absolute pink `cp-badge` was replaced by an inline `.cp-pop` pill inside the h3 (cards now use `overflow:hidden` for the watermark). Removed `.cp-hrs`/`.cp-badge` styles. Verified: 5 cards render, 2-col desktop grid + wide Custom System span + chat box beside, single-col mobile, no console errors, no horizontal overflow.
- **2026-07-03 (pricing quiz + contact page hardening)** — Quiz: booking answers no longer force the Custom Web App recommendation (stays an add-on unless the visitor chose a custom tool); Back restores the stored answer; sessionStorage resume; focus management + `aria-pressed`/progressbar semantics; single-column options + hidden key chips on touch. Contact: honest submit success/error (inline error + mailto fallback, no more fake "sent"), GSAP dependency removed (vanilla reveal/magnetic/success), fieldset/legend for all option groups, success focus, `?addons=` preserved across mode toggles, ContactPage/Organization JSON-LD.
- **2026-06-29 (services preview option 03 responsive padding)** — Tuned `services-section-alternatives-preview.html` Option 03 / Capability Bento responsive spacing. At narrower widths, the stage padding now steps down to `16px`/`12px`, cards step down from `24px` to `20px`/`18px`, card gaps tighten slightly, and mobile min-heights are reduced so the bento feels less over-padded while keeping the pills, tags, CTA, and ghost numbers intact. Verified current browser width and 390px phone width: no horizontal overflow and cards fit inside the stage.
- **2026-06-29 (services preview option 03 no-gradient tweak)** — Removed the gradient from `services-section-alternatives-preview.html` Option 03 / Capability Bento hover wash. The hover background is now a flat platform-tinted fill using `color-mix`, while the bento structure, tags, CTA, icon motion, focus dimming, and ghost-number layering remain intact.
- **2026-06-29 (services preview option 03 full polish)** — Upgraded `services-section-alternatives-preview.html` Option 03 / Capability Bento with the requested full improvement pass: staggered bento entrance, hover focus dimming, hover accent wash, icon pop/rotate, ghost-number parallax, platform-colored icon tiles, `Best for` pills, scope tags, softer oversized number watermarks, and a Shopify hero-card CTA (`See Shopify work →`). Verified cache-busted render: six cards, six best-fit pills, twelve tags, Shopify CTA present, ghost number remains behind content, no console errors, and mobile cards fit without horizontal overflow.
- **2026-06-29 (services preview option 03 layer fix)** — Adjusted `services-section-alternatives-preview.html` Option 03 / Capability Bento so the large ghost service numbers sit behind the card content instead of above it. Added `isolation:isolate` on `.v3-card`, pushed `.v3-card::after` to `z-index:0`, and lifted direct card children to `z-index:1`.
- **2026-06-29 (homepage services alternatives preview)** — Added `services-section-alternatives-preview.html`, a focused scratch page for redesigning the homepage "What we help with" section shown in the latest screenshot. It keeps the same six services and descriptions, then explores ten directions: Editorial Service Index, Command Board, Capability Bento, Scope Builder, Platform Cards, Comparison Matrix, Service Tickets, Browser Tabs, Process Ladder, and Magazine Spread. Verified the page serves locally, all 10 options render, each option receives the six-service data set, desktop/mobile have no horizontal page overflow, and no console errors were reported.
- **2026-06-28 (about stats option 10 shipped)** — Shipped the `stats-preview.html` Option 10 "Bento Scoreboard" direction into `about.html`. Replaced the old three equal stats cards with an asymmetrical proof bento: a large portfolio stat card (`44+`) and a stacked side panel for `3` platforms and `1` proposal-first process. Kept the existing `[data-count]` hooks so shared `work.js` count-up behavior still applies, and added responsive mobile stacking.
- **2026-06-28 (stats preview expanded)** — Expanded `stats-preview.html` from five to ten stats-band alternatives. Added Proposal Ledger, Layered Proof Stack, Ticker Wall, Constellation Map, and Bento Scoreboard options, updated the preview title/nav/intro to "10 directions", kept the shared 44+/3/1 stat data, added reveal/count-up wiring for the new layouts, and hid the crowded jump nav on medium screens.
- **2026-06-28 (CTA ticket improvements preview)** — Added `cta-ticket-improvements-preview.html`, a focused scratch page with five improved versions of the CTA ticket/coupon section shown in the latest screenshot: Premium Split Ticket, Proposal Desk, Claim Band, Command Card, and Stamped Proposal Pass. The set borrows from `ticket-preview.html` and `cta-coupon-alternatives-preview.html` but narrows the exploration to the homepage CTA section only, with stronger stubs, clearer action areas, responsive layouts, and reveal animation fallbacks.
- **2026-06-28 (radial orbit alternatives preview)** — Added `radial-orbit-alternatives-preview.html`, a dedicated scratch page with five different radial-orbit process directions: Docked Compass, Solar Glass Orbit, Radar Sweep, Constellation Map, and Orbit Ledger. Each keeps the same four-step process copy but changes the visual language, card structure, animation, and interaction model. Verified desktop cards stay inside their stages, mobile has no horizontal overflow, and Orbit Ledger step switching works.
- **2026-06-28 (process orbit preview animation polish)** — Improved `process-orbit-preview.html` animation behavior: reduced desktop orbit size slightly so more of the sequence fits, synchronized/restartable comet motion via `beginElement()`, added a short guided card-focus tour during entrance, throttled pointer tilt with `requestAnimationFrame`, added keyboard focus parity for cards, respected reduced-motion in JS, and added a mobile stagger fallback instead of making the mobile list fully static. Follow-up polish fixed the card-icon bug by scoping the main orbit SVG selector to `.orbit > svg`, then redesigned the step cards twice; latest direction is a sharper "orbital control board" module style with flat 6px corners, colored phase rails, ghost step numerals, circular icon pucks, dark active-state cards, and side-card offsets so the hub no longer covers copy.
- **2026-06-28 (about page revamp)** — Revamped `about.html` with a stronger trust/story structure: added hero proof chips, replaced stale "free audit" copy with "free consultation", aligned stats to the current 44-project portfolio proof, added "Who we help" audience cards, added a 4-step process section, refreshed the values grid with inline icons, and updated the CTA ticket to "Start your project".
- **2026-06-28 (contact footer wordmark alignment)** — Fixed `contact.html` footer watermark alignment by adding the missing `text-align: center` to its inline `.foot-mark` rule, matching the homepage `styles.css` and shared `work.css` footer behavior.
- **2026-06-27 (contact form full UX revamp)** — Implemented the requested 10-part contact form upgrade on `contact.html`: progressive 3-step flow, card-based service picker with icons, two-column service/detail workbench, "Helpful" badges on recommended fields, pill-style checkbox toggles, segmented timeline control, live request summary, collapsible optional notes, compact quote-request layout, and retained Netlify-compatible hidden/select-backed submission behavior. Inactive service panels remain disabled so only relevant adaptive answers submit.
- **2026-06-27 (contact project-details neutralized)** — Removed the colored accent rail/special border treatment from the adaptive `Project details` card on `contact.html`; it now uses a neutral `var(--line)` border, flat white background, and softer shadow while keeping selected checkbox outlines for usability.
- **2026-06-27 (contact form structure cleanup)** — Improved the `contact.html` form design with three numbered sections (Contact info, Project type, Timing & notes), a stronger final action area, and less redundant questioning. Removed the generic `project_size` select because adaptive panels already ask service-specific product/page/feature/stage questions; removed the duplicate advice-only timeline field and kept one universal timeline field.
- **2026-06-27 (contact form visual polish)** — Removed the gradient from the adaptive `Project details` panel on `contact.html` and replaced it with a flat white card, subtle border/shadow, header divider, pink-dot kicker, and clearer checked-state styling for checkbox cards.
- **2026-06-27 (contact adaptive questionnaire)** — Revamped the `contact.html` form so "What do you need?" reveals a focused **Project details** panel with service-specific follow-up questions. Added conditional fields for Shopify revamps, new Shopify/ecommerce stores, WordPress/WooCommerce, Webflow, business redesigns, maintenance/support, custom apps, and "not sure yet"; inactive panels are hidden and disabled via JS so Netlify submissions only include the relevant answers.
- **2026-06-27 (pricing eyebrow consistency)** — Standardized the pricing/package small header labels to match the section eyebrow treatment (`13px`, `700`, `0.18em`, uppercase, pink): package detail `.pk-eyebrow`, modal `.pk-kicker`, optional-extras/good-to-know card headings, and quiz result kicker.
- **2026-06-27 (pricing add-ons modal caption)** — Updated `packages.css` so the `.pk-kicker` caption inside pricing compare/add-ons modals uses the KNB pink accent (`var(--pink)`), fixing the "Optional extras" modal caption from inheriting dark body text.
- **2026-06-27 (pricing selector alternatives preview)** — Added `pricing-section-alternatives-preview.html`, a standalone scratch page with **5 alternative layouts** for the pricing/package selector section shown in the user screenshot: command selector, comparison matrix, package journey, scope builder, and proposal desk. It keeps KNB's homepage/pricing visual language, no-public-pricing rule, website/care toggle framing, package data, interactive package selection, builder-style recommendation, and responsive/reduced-motion handling.
- **2026-06-27 (hero card alternatives preview)** — Added `hero-card-alternatives-preview.html`, a standalone scratch page with **5 complex alternatives** for the homepage platform card hover/tap interaction: physical edge flip, split-door reveal, layered blueprint stack, command cube, and peel-back project brief. The preview aligns with the homepage visual language (off-white grid, oversized editorial type, Satoshi, platform gradients, simpleicons, KNB nav mark), includes Shopify/Webflow/WordPress platform switching, tap-to-toggle behavior, pointer tilt, reduced-motion guards, and a long copy-paste prompt section.
- **2026-06-25 (platform pages — major content + animation revamp)** — Added **6 new sections** to shopify/webflow/wordpress (CSS in `work.css`, count-up in `work.js`, content per page): **branded hero glow** in the platform colour (`--pcol` set per page on the inline style + `.page-hero::after` radial), a **stat band** (count-up: project count / 4.2 yrs / ★4.9 — *rating is placeholder, flag*), a **"When [Platform] is the right call"** positioning section with best-for chips + **cross-links to the other two platforms** (good for SEO/UX), a **"Built with" tech strip**, a **flagship spotlight** (one featured project, big, with real mShots screenshot + Visit-live), a **platform-specific FAQ** (`<details>` accordion), and a **pricing teaser** band. Plus **project-card image zoom on hover**. Conversion order: hero → stats → why → what-we-do → tech → flagship → grid → testimonials → FAQ → pricing teaser → CTA. Stat count-up uses `data-num` (not `data-count`) to avoid the existing integer count-up. ⚠️ The **"★4.9 average rating"** is a placeholder like the testimonials — needs a real rating before launch.
- **2026-06-23 (single dynamic header — one nav for all pages)** — The nav had drifted out of sync (About & 404 said "Free audit" vs everyone else's "Start your project"; contact's CTA pointed at `#contactForm`; active states hand-set). Replaced the duplicated `<nav>` + `<div class="mobile-menu">` on **all 10 pages** with one line — `<script src="header.js"></script>` — and created **`header.js`**: it derives the current page from `location.pathname`, builds the full nav + Work mega-menu + mobile menu with the correct **active state** (Work on portfolio/shopify/webflow/wordpress/case-study; Pricing/About/Contact on their pages), a uniform **"Start your project"** CTA, and a context-aware logo href (`#top` on home, else `index.html`), then injects it synchronously at the script's position (`document.currentScript.insertAdjacentHTML`). Because it runs mid-`<body>` before the end-of-body scripts, **navmenu.js + main.js/work.js still initialise it** (mega-menu hover/filters, burger, nav-scroll). **Now edit the nav in ONE place (`header.js`).** Verified via headless renders (Contact active on contact, Work active on shopify, mega-menu intact). ⚠️ **SEO note:** nav links are now JS-injected (not in static HTML); mitigated because the footer + `sitemap.xml` still expose every page statically, and modern crawlers render JS. The 3 `*-preview.html` dev files keep their own inline navs.
- **2026-06-23 (platform-page polish + accessibility pass)** — **Platform pages:** fixed the 2 leftover **picsum placeholders** on `wordpress.html` (All Terrain + Privvy now use real mShots — so all 44 project cards use real screenshots); added a **"What we do" section** to shopify/webflow/wordpress (`.svc` cards in `work.css`, 4 tailored service cards each, between hero and the project grid); **moved the All Terrain card to last** (behind load-more) because its site is **Cloudflare-protected** — both mShots AND headless Chrome only get the "security verification" page, so it can't be auto-captured (needs a hosted screenshot). **Accessibility:** audited — `lang`, single h1, alt text, form labels, reduced-motion all already good. Added: **skip-to-content link** on all pages (`.skip-link` + per-page `#main`/`#top` target), a **global `:focus-visible` outline** (pink, in both stylesheets — work.css only had 1 focus rule), and **`aria-hidden="true"` on all decorative inline SVGs** site-wide (82 on index alone). Muted body text contrast ≈ 4.7:1 (passes AA). ⚠️ **Domain:** the live site is on **`knb-solutions.netlify.app`** but the SEO pass used `https://knb.solutions` for canonical/OG/sitemap — confirm the final domain and update if needed.
- **2026-06-22 (logo → "Option 1" wordmark + pink dot)** — Reviewed 5 animated logo options in `logo-preview.html` (kept for reference); user picked **Option 1**: a bold **"KNB."** wordmark with a pink signature dot, no badge. Nav markup changed from `<span class="mark">K</span>KNB` to `.lw` wordmark (`.ch` letter spans + `.ld` dot) on all 10 pages; footer "KNB" got a matching `.lgdot`. CSS added to **both** `styles.css` + `work.css`: `.logo .lw` (weight 900, tight tracking), eases in on load (`lwIn`), hover = letters wave (`lwWave`) + dot bounce (`lwDot`), reduced-motion guarded. **Favicon** updated to the K badge + pink dot (`favicon.svg`/`favicon-square.svg`) and all sizes regenerated (16/32/180/192/512 + `.ico`). The old `.logo .mark` CSS is now dead (harmless). Verified via headless render.
- **2026-06-22 (SEO pass — meta descriptions, OG/Twitter, sitemap, robots, JSON-LD)** — Audit found index/contact/shopify/webflow/wordpress had **no meta description**, 7/8 pages had **no og:image** (the one that did pointed at a nonexistent `og-portfolio.jpg` on the wrong domain `knbwebsolution.com`), and there was no sitemap/robots. Fixed: a Python pass **strips + rebuilds a consistent SEO/social block** on all 10 pages (description, `canonical`, `og:type/site_name/title/description/url/image`, `twitter:card/title/description/image`) — descriptions kept where they existed, written fresh where missing. Generated a branded **`og-image.png`** (1200×630, dark card + K badge + "Websites that earn their keep." — from `og-image.svg` via rsvg-convert). Added **`sitemap.xml`** (8 public pages) + **`robots.txt`** (disallows the 3 preview files + case-study, links the sitemap) + **ProfessionalService JSON-LD** on index. ⚠️ **Domain assumed `https://knb.solutions`** (matches the email) — if the live domain differs, update the canonical/og:url/sitemap/robots URLs. (Per user, skipped audit items #1 form, #2 testimonials, #3 domain-confirm beyond this, #4 screenshots, #5 industries, #9–14.)
- **2026-06-22 (added 26 WordPress projects → catalogue now 44)** — Client sent 26 live URLs; **confirmed all 26 are WordPress** (verified by fetching each + checking for wp-content/wp-json markers — user also confirmed). Added them to **`wordpress.html`** (now 28 WP cards: the 2 originals + 26 new, real **mShots** screenshots) and the **`portfolio.html`** grid (now **44 `<article>`s** total: 6 Shopify + 10 Webflow + 28 WordPress; work.js builds their mShots from `data-url`, filter pill counts + "Showing N of 44" auto-update). Bumped counts site-wide: nav dropdown `All work 18→44` / `WordPress 2→28` / `"18 projects · 3 platforms · 12+ industries" → "44 · 3 · 18+"`, testimonial proof `18→44`, portfolio hero stat `18→44`. **Titles** derived from the domains (factual brand names). ⚠️ **Industry/category labels are INFERRED from the brand names** (e.g. Coptrz=Drone tech, Millers Bakery=Food, Phoenix Safe=Security) — confirm with the client before launch; also confirm permission to feature each. URLs: digitallydisruptive, mdo.digital, coptrz, influencercollective, barwonheatpumps, tgilescaravans, coachman, amberarch, edeninternational, rushdrink, hatbag, davidwalters, cocuun, gsfgroup, millersbakery, sasgas, stephan-uk, financialadvisoruk, sbskills, learn.orca, signum-solutions, onlinerecordingstudio, thewalsallbox, ten87.studio, phoenixsafe, zerogravitysafety.
- **2026-06-22 (nav "Work" mega-menu → final = editorial / "Work 5" design)** — Reviewed 5 full-width alternatives in `work-menu-preview.html` (kept for reference); user picked the **editorial** one. Shipped it across all 10 pages via shared `navmenu.css` + `navmenu.js`. Panel = **"Browse" sidebar** (platform links w/ counts + inferred industry chips) + **"Sites we've shipped"** main with **working platform filter chips** (JS filters the thumbs by `data-plat`), **8 real mShots thumbnails** (hover "View →" + zoom), and a footer stats line. **Two root-cause fixes:** (1) the **circle bug** — `.nav-links a` (pill-link rule, border-radius/padding) was cascading onto the panel's `<a>` cards; changed it to **`.nav-links > a`** in `styles.css` + `work.css` so only top-level links get it. (2) **panel positioning** — the nav's `backdrop-filter` made it the containing block for `position:fixed` panels (and corrupted the flex row); panel is now `position:absolute` against `.nav-inner` (`position:relative`), full nav width. Responsive (3-col <1280, hidden <1080 → mobile chip row). ⚠️ Placeholders: industry tags + the "18 / 12+" counts are inferred — confirm; mShots are live third-party (slow first paint) — swap for hosted screenshots before launch.
- **2026-06-22 (nav revamp — "Work" mega-menu + platform pages added)** — Added a rich **Work dropdown** to the nav on **all 10 pages**. Shared **`navmenu.css` + `navmenu.js`** (self-contained, layered over the existing `.nav` in styles.css/work.css). The "Work" trigger opens a **mega-menu panel** (660px). v2 design (the icon-tile version was rejected as too plain): a **header row** ("Our work · by platform" + "View all 18 →"), then **three image cards** — each a real **mShots preview screenshot** (Light My Bricks / Feedbird / All Terrain Industries) with a brand-colour logo badge, name, description, and project count ("6 / 10 / 2 projects →"), hover = lift + image zoom + brand-colour border; and a **footer bar** (pulsing "Now booking" dot + "Start a project →"). Platform pages previously had **no nav entry**. Brand-tinted gradient placeholder behind each thumb covers the mShots load. Desktop = CSS `:hover` (with an invisible hover-bridge); touch/keyboard = `navmenu.js` click-toggle + Esc/outside-click close + `aria-expanded`; panel left-aligned to the trigger so it never clips. Active state: the trigger is `.active` on the work pages (portfolio/shopify/webflow/wordpress/case-study); Pricing/About/Contact keep their own active. **Mobile menu** gained a `.mm-plats` chip row (Shopify/Webflow/WordPress) under Portfolio. Rebuilt `.nav-links` + `.mobile-menu` on every page via script; `#mobileMenu` id + burger JS untouched. Hidden below 900px (mobile menu takes over). All pages serve 200.
- **2026-06-22 (care plans, CTA seam, FAQ font, testimonials ported to work-pages)** — **Care plans (`pricing.html`):** kept hours **confidential per SOP** — added a **"Monthly support time"** row with *relative* labels (Light/Standard/Priority/Extended/By agreement, NO hour counts) + an **"Unused time rolls over"** row (✓ Growth & Premium). Replaced the tiny grey footnote with a 4-card **"how it works" explainer** (`.care-info`): included support time, **rollover** (highlighted, pink "Growth & Premium" tag), bigger-work-quoted-first, flexible/no-lock-in. (User asked to publish hours; I flagged the confidentiality rule and they chose to keep hours private.) **CTA ticket (`work.css`):** briefly tried a clean notch-hole perforation, but the user prefers the **original rip-on-hover** ticket (jagged clip-path tear + 3D `rotateY` peel) — **reverted to that** (the committed version). Leave the rip as-is unless asked. **FAQ question font** reduced (too big): home `.faq-q` `clamp(18,2vw,24)` → `clamp(15.5,1.4vw,18)` (weight 600); pricing `.pfaq summary` `clamp(16,1.8vw,20)` → `clamp(15,1.4vw,17.5)`. **Testimonials ported to other pages:** extracted the home marquee into **shared `testimonials.css` + `testimonials.js`** (self-contained; depends only on work.css tokens; count-up degrades gracefully without gsap; reveal uses work.js's `.reveal`→`.in` observer). Injected the `#testiWall` section + `#tqModal` + the css/js links into **about, shopify, webflow, wordpress** (before each `.cta-strip`), and **replaced** the old placeholder testimonial blocks on **portfolio** (`.testi-sec` 3-card grid) and **pricing** (`.trust` band — note this dropped its 18/3/12+ count-up stat band; re-add if wanted) with the same marquee. So all marketing pages now share one testimonials section. Home still uses its inline copy in `styles.css`/`main.js` (two sources of truth — edit both, or refactor home onto the shared files later).
- **2026-06-22 (favicon / brand icon set)** — Generated a favicon matching the nav mark (cream "K" on an ink `#111110` rounded badge). Master `favicon.svg` (rounded) + `favicon-square.svg` (full-bleed, for iOS/Android masking); rasterized with `rsvg-convert` → `favicon-16/32.png`, `apple-touch-icon.png` (180), `icon-192/512.png`; `favicon.ico` (32px) via `sips`. Added `site.webmanifest` (name/short_name/theme `#111110`/bg `#f4f3ef`/icons). Injected the icon `<link>`s + `<meta name="theme-color">` into all 11 HTML `<head>`s (relative paths, after the charset meta). To regenerate: edit `favicon.svg`/`favicon-square.svg`, re-run the `rsvg-convert`/`sips` lines.
- **2026-06-22 (testimonials 10 upgrades + names, email, FAQ + footer revamp)** — **Home testimonials (`#testiWall`): all 10 polish items.** CSS (`styles.css`): #1 hover-focus (row dims, hovered card stays), #5 gold star **shimmer** on hover, #6 decorative **quote-mark** glyph per card, #7 **avatar ring** in the platform colour (custom prop inherits to the avatar; re-added `--pa` per `[data-plat]`), #8 **featured/spotlight** card (`.feat` → dark card, first Shopify entry, "★ Featured" pill), #9 faint **"CLIENTS" watermark** behind the section, #10 **quote modal** (`.tq-*`). JS (`main.js`): #2 **scroll entrance** (added `reveal` to both `.talt-mq` rows — they animate via the generic reveal since `.talt` isn't in `customSel`), #3 **count-up** on the proof figures (4.9 / 18, IntersectionObserver + gsap, decimals), #4 subtle **3D tilt** on card hover (yields to drag via a shared `dragging` flag), #10 **click→modal** wiring (cards now open the modal instead of navigating; deep-link href kept for right-click). All reduced-motion guarded. **Testimonial names** now pair **invented client names with real KNB project brands** (Light My Bricks, Feedbird, All Terrain Industries, Cacao Collective, Maestro Labs, Rise Outdoor, Privvy Marketing, Teamtown) — no more `[Client name]` placeholders. ⚠️ **SOP:** names + quotes are illustrative on real brands — get written permission + real quotes before launch. **Email:** `youremail@gmail.com` → **`info@knb.solutions`** across all 10 pages. **FAQ revamp** (`index.html` + `styles.css`): 2-column layout (sticky intro aside w/ eyebrow + "Book a free consultation" CTA | accordion), **3 new Qs** (cost — no prices per SOP / ownership / content & images), and a **FAQPage JSON-LD** block for SEO. **Footer revamp** (`index.html` + `styles.css`): CTA band ("Let's build something worth visiting"), 3 nav columns (Explore / Platforms / Get in touch), pulsing **"Now booking" status** dot, real email, **oversized "KNB" wordmark**, and a **Back to top** link (smooth-scroll handler in `main.js`). **Note:** the home CTA/pricing/new-page work from the prior session was uncommitted — this commit persists it (the user's "broken CTA" screenshot was just stale cached CSS; `work.css` `.ctaC` styles are correct).
- **2026-06-21 (home testimonials — picked Option B marquee, fully built)** — Removed the A/C/D comparison options + the old slider section (and its unguarded `sliderTrack` JS that would've thrown). Promoted the **marquee** to the real testimonials section (`.testi-wall`, heading + "★ 4.9 · 18 projects shipped" proof line). Rebuilt it **JS-driven** (`#testiWall`, data array in `main.js`): two rows, opposite directions, **rAF-based** (not CSS anim) so it supports **drag-to-scroll** (pointer, with click-suppress after drag), **pause-on-hover**, **speed-up while the page scrolls**, and **prefers-reduced-motion** (no auto-drift, drag still works). Each card now has a **platform chip** (Shopify/Webflow/WordPress), a **left accent bar** in the platform colour, **varied widths** (`.wide`), and **deep-links to `portfolio.html#work=<platform>`**. `touch-action: pan-y` keeps vertical page scroll working. **Still placeholder (SOP):** quotes/names/initials and the "4.9" rating — swap for real, attributable content + a real/sourced rating before launch. Dead A/C/D-only CSS remains (harmless; prune later).
- **2026-06-21 (pricing — "Pick a starting point" selector redesign)** — Fixed the flat/disconnected feel (post pink-removal) using the per-type accent colours, not pink. **Panels now carry the active type's accent** — set `--pc/--pcbg` on `.psel-panel` (ecom green / app blue / others neutral), so the **eyebrow + quote dot** colour up and the **detail-panel corner glow** matches the selection via `.psel-detail:has(.pc-ecom.on)` etc. Added a **package icon tile** (`.pp-ic`) at the top of each panel; a **fact-chips row** (`.pp-facts` — delivery est. + revisions/support, complementing the Included list); turned **"Quoted to your scope" into a bordered pill**; made the **active card tint** (`background: var(--pcbg)`) + its icon fill with the type colour; gave cards a **hover lift**; made "Included" h4 ink (stronger) vs muted "Quoted separately"; and changed the **"Popular" badge to outlined** (white/ink) so it reads against the dark buttons. `:has()` is used (universal 2026 support; falls back to a neutral glow). Home Solutions "See full details" button also got a green dot to match the primary. Pricing serves 200.
- **2026-06-21 (home Solutions — 10 animation/design adds)** — All reduced-motion guarded. **#1** sliding toggle indicator (`.solu-ind`, JS measures the active tab's width/offset; `.ready` class avoids a pre-JS invisible-text flash). **#2** directional panel entrance (web slides from left `soluInL`, care from right `soluInR`). **#3** detail panel swaps now animate — JS adds `.swapping` to the aside → name/desc/fact fade+rise in. **#4** tag chips stagger-in (`tagIn` keyframe on the recreated `<span>`s). **#5** active card left **accent bar** (`::before` scaleY) + border/shadow. **#6** icon micro-motion (scale+rotate, fills with the type colour when active). **#7** choreographed scroll-in — aside now slides in from the right after the cards (GSAP). **#8** connector **nub** (`.sol-item.active::after` triangle) pointing at the detail panel, desktop only. **#9** faint **blueprint grid** behind the section (`.plans::after`) + the pink glow **re-blooms** on toggle (reuses the existing `.plans.bloom`/`pkgBloom`). **#10** a small **fact pill** in the detail panel (delivery est. for websites / "Rollover included" etc. for care — SOP-safe, from a JS `FACT` map, no hour counts/prices). Files: `index.html` (indicator + fact markup), `styles.css`, `main.js`.
- **2026-06-21 (portfolio + home + pricing tweaks)** — **Portfolio:** removed the count-up **rings** on the hero stats (plain numbers, original size restored); **filters revamp** — styled the previously-unstyled search box (`.work-search`) + sort + **industry strip** (`.ind-strip`/`.ind-chip`), and added **brand-colour dots** to the platform pills; **alternative "Get started" CTA** added ON TOP of the existing one for comparison (`.cta-alt` — centered dark card w/ dotted texture, green check trust row; delete one once chosen). **Home Solutions aside:** detail-panel buttons are now **2 columns** (full-width only ≤600px) and the primary button is white-on-dark (was invisible ink-on-ink). **Pricing:** pink is now **text + dots only** — neutralised the business `--pc` accent, featured borders/gradients, icon tiles, the 3px bars, cell fills, the quiz/row hovers, the cursor glow, and the ambient hero/quiz glows (all → ink/neutral/white). Pink remains only on eyebrows/kickers/labels + the small round dots.
- **2026-06-21 (pricing — toned down pink + home→pricing deep-links)** — **Pink subtler:** the loud full-saturation pink is now neutral/ink — featured & business `.btn`s → `var(--ink)`, the "Popular"/"Most popular" badges (`.pr-ribbon`/`.crow-badge`/`.pi-tag`) → ink fills, the big quiz/hero radial **glows** dropped to ~0.07–0.14 opacity, and the quiz-option hover pink softened. Pink now only survives as small accents (eyebrows, dots, thin 3px bars, support-level bars, the per-type business `--pc`). **Deep-links:** the home Solutions cards (and the right-panel "See full details" button, updated live by the hover JS) now link to `pricing.html#build-<pkg>` / `#care-<plan>` (e.g. Ecommerce → `#build-ecom`). `pricing.html` has a new handler that reads that hash on load → clicks the right **Website projects/Care plans** tab + the matching **package** in the selector, then scrolls to it (offset for the nav). Care deep-links open the Care tab (no per-plan selector there yet). `main.js` + pricing inline JS verified.
- **2026-06-21 (home Solutions — transferred the pricing card design)** — Per feedback the merged section now uses the **exact pricing-page selector card design** (`.sol-list`/`.sol-item`/`.sol-ic`/`.sol-tag`, transferred to `styles.css`). **Removed the inline detail panel** ("the most detailed part") and the verbose `.solu-head`/`.care-intro` — section is now just heading + toggle + a card list per tab + one footer button. Each card and the footer button **link to `pricing.html`** for full details; footer reads **"See full details →"** (NOT "see pricing" — homepage shows no prices). Cleaned the `.plans` GSAP reveal to target `.sol-item` (the old `.pkg`/`.mrow`/`.plans-sub` targets are gone).
- **2026-06-21 (portfolio — 6 discovery/UX adds)** — Built 6 of a 10-idea list (the other 4 need real assets — see below). **(1) Project search** — `#workSearch` input; `matches()` in the `work.js` controller now also checks title/scope/tags. **(2) Deep-linkable projects** — modal `open()` sets `#project=<slug>`; on load, a matching `#project=` opens that card's modal; **Copy-link** button (`#pfmShare`). **(3) Modal prev/next** — `#pfmPrev`/`#pfmNext` + ← → keys cycle visible cards. **(4) Industries strip** — `.ind-strip` chips (Retail/Food/Software/Property/Outdoor/Beauty) drive `setFilter` (industry slugs already tokenised onto cards); active-state synced. **(10) Modal "Start a project like this →"** CTA (`#pfmStart` → `contact.html#<plat>`); a11y (focus-trap/ESC/scroll-lock) already existed. Modal also clears the hash on close. CSS is an **inline `<style>` in `portfolio.html`** (portfolio-scoped: search, ind-strip, `.pfm-nav`, `.pfm-share`); JS edits are in the shared `work.js` controller + modal IIFEs. **Deferred (need real content, won't fabricate):** before/after per project (no per-project "before" shots), in-context testimonials + per-project outcomes (need real quotes/results), real client logos. Verified live on :8010, no console errors. ⚠️ Both sessions are editing `work.js`/`portfolio.html`/`pricing.html` — watch for clobbering.
- **2026-06-21 (home — merged Plans + Maintenance into ONE toggle section)** — Per request (modelled on the pricing page): the separate **Plans** ("Solutions for every stage") and **Maintenance** sections are now a **single `<section class="plans" id="plans">`** with a **Website projects / Care plans toggle** (`.solu-toggle`/`.solu-tab` → swaps `.solu-panel`s; JS in `main.js`). Website panel keeps the existing `.pkg` selector; Care panel holds the existing `.mrow` care tiers (now light cards on the light bg — fine). Added a **"See full pricing →"** ghost button in the header + a foot with **"Start your project"** + **"See full pricing & plans →"** → `pricing.html`. The `.plans` GSAP reveal now also covers `.solu-toggle` + `.mrow` (they're excluded from the generic reveal). Dead now: `.maint-alt*` / `.kick` / `.maint-alt-foot` CSS in `styles.css` (section removed) + the `.maint` ScrollTrigger in `main.js` (no longer matches anything). Also redesigned the **pricing-page Add-ons** (`.addon` got icon tiles + hover top-accent + a dotted "quoted by" badge; `.pr-note` "Good to know" got a pink eyebrow + left accent + dot markers).
- **2026-06-21 (portfolio.html — perf + polish batch of 7)** — #1 **lazy-load** the 18 mShots screenshots via IntersectionObserver (`rootMargin:300px`; works with load-more/filter since hidden cards load when shown); #2 **counts on filter pills** ("Shopify (6)", `.pf-n`); #3 **scroll-progress bar** (`#scrollProg`) + **back-to-top** (`#toTop`, shows past 600px); #4 **modal focus-trap** (Tab cycles inside the dialog) + modal image falls back to building the mShots URL if a lazy shot isn't loaded yet; #5 **hover auto-pan** on card screenshots (img height 116% + translateY on hover); #6 **before/after hint** — the spotlight slider auto-nudges once on scroll-in; #7 **heading underline draw-in** on section reveals. All guarded by reduced-motion/touch. `node --check` passes.
- **2026-06-21 (pricing — packages & care redesign #2)** — Per feedback, redid both: **Website packages → interactive selector** (`.psel` — list of 4 on the left, detail panel swaps on the right with Included vs Quoted-separately columns; per-type accent via `.pc-*`; Business tagged "Popular"; click + desktop-hover, JS IIFE in `pricing.html`). **Care plans → comparison table** (reuses `.cmp`; 12 rows × 5 tiers, Growth column `.feat`-highlighted, ✓/—/text). Replaced the prior per-type accent card grid and the level-bar `.crow` rows (those CSS blocks are now unused/dead — safe to prune). Also: my preview now runs on **port 8010** (terminal holds 8000 with a Python server) — `.claude/launch.json` updated. No console errors.
- **2026-06-21 (portfolio.html — 18 features/animations batch)** — Implemented the full "give me 10 + 10" list. **Features:** A1 **sticky filter bar** (`.work-controls`, `position:sticky`); A2 **load more** (9 at a time, `#loadMore`); A3 **sort** (`#workSort`: Featured/A–Z/Platform, reorders via `appendChild`); A4 **real client wordmarks** in the logo marquee (the 18 clients — confirm logos/permission); A7 **industry filtering** (industry chips are now clickable; their slug is registered as a `data-tags` token); A8 **Process section** (`.process-sec`, 4 steps); A9 **FAQ** (`.faq-sec`, native `<details>`, last Q links to pricing); A10 **SEO** (og:url/og:image/twitter:image — placeholder absolute URLs, swap on launch). The work grid is now driven by one controller in `work.js` (filter + sort + load-more + FLIP + count + `#work=` hash). **Animations:** B1 cursor spotlight on `.work`; B2 hero headline word-rise; B3 magnetic `.btn`s; B4 tilt on `.proj-img`; B5 conic count-up **rings** on hero stats (`--p` driven by the count); B6 marquee speeds up while scrolling; B7 section separation via the ambient backgrounds; B8 drifting **"WORK" watermark**; B9 hero gradient blobs (from the prior pass); B10 **FLIP** animation on filter/sort. All hover/motion effects guarded by `(hover:none)` / `prefers-reduced-motion`. **NOT done (need real content — SOP):** A5 per-project case studies (don't fabricate problem/approach/outcome) and A6 real testimonials (the 3 are still `[Client name]` placeholders). `node --check` passes; **needs a real-browser pass** (FLIP, sort, load-more, sticky, tilt, rings).
- **2026-06-21 (portfolio.html — chip tags + per-section animation)** — **Card tag chips** now match the home Projects design (pill row: colored platform chip · category · dashed industry). Built in `work.js` from each card's data (no markup bloat) — removes the old plain `.cat` label, appends `.pf-tags` with `.pf-chip`/`.is-plat`/`.is-type` (styled in `work.css`, platform chip uses the card's `--accent`); the platform chip stays the clickable filter (`button.pf-chip[data-filter]`). **Industry words are a JS map (`INDUSTRY`) — inferred, confirm with client.** **Per-section animation pass** (all reduced-motion guarded): reveal-variety modifiers `.r-left/.r-right/.r-scale/.r-pop` so each section enters differently (spotlight image scales in + copy slides from right, work-head slides left, testimonials pop, CTA scales); ambient backgrounds — hero drifting colour blobs, spotlight pink glow, work-grid faint dotted backdrop, CTA corner glow. `node --check` passes.
- **2026-06-20 (portfolio.html — ported the home Projects upgrades)** — Brought the home "Recent websites" treatment to the full portfolio (uses `work.css`/`work.js`). **Cards** converted from `<a>` wrappers to `<article class="proj-card">` carrying `data-url` (the real live site), `data-tags` (platform + goal tokens), `data-title`, `data-scope`; each keeps a real `<a class="view-tag">` to the live site (SEO-safe) + a "View details" button + a clickable `<button class="cat">` chip. **Real screenshots** via WordPress mShots built from each card's `data-url` (`work.js`), with the old picsum as `data-fallback` + `.pf-skel` shimmer/blur-up. **Filters:** added the goal axis (Online store/Business site/Editable CMS) beside the platform pills in one row (`.proj-fdiv` divider), token-based matching via `data-tags`, a visible `#workCount` ("Showing N of 18"), the existing `#filterStatus` sr-only kept, an empty state, and `#work=<key>` hash (clickable cat chips + filter buttons set it; read on load). **Detail modal** (`#pfModal`, `.pfm-*` in `work.css`) opens on card click / "View details" — large screenshot + Screenshot/Live-preview iframe toggle + scope + Visit-live button + Esc/backdrop close. Clicks on the Visit-site link or cat chip are excluded via `[data-stop]`. The 18 real URLs the client sent match the existing card hrefs. `node --check` passes; needs a real-browser pass. Same caveats as home (mShots "generating…" placeholder on first hit; live-preview blanks for sites that block framing). The hero `.pf-stats` (18/3/12+) + before/after spotlight + placeholder testimonials/logo-marquee were left as-is.
- **2026-06-20 (home Projects — design tweaks)** — Per feedback: **removed the stat band** (`.proj-stats` "18/3/6 + Recent clients" white card — markup + CSS gone; this drops feature #9). **Removed the "Platform"/"What you need" filter labels** and merged the two groups into **one filter row** (`.proj-fdiv` thin divider between platform pills and goal pills); the goal pills (#3/#7) are kept. **Fixed card-tag alignment** — the platform chip is a `<button>` among `<span>`s, so `.pj-chip` is now `inline-flex`/`line-height:1` and `.pj-tags` is `align-items:center`. (mShots screenshots confirmed working live.)
- **2026-06-20 (home Projects — 10 features + redesign)** — Rebuilt the "Recent websites & store revamps" section with all 10 requested features. **#1 real screenshots:** cards now pull live thumbnails from **WordPress mShots** (`s.wp.com/mshots/v1/<enc-url>?w=1200`, no key) built in `main.js` from each card's `data-shot`; **picsum stays as the `data-fallback`** on error. **#2 live preview:** in the modal, a Screenshot/Live-preview toggle injects an `<iframe>` of the site (with an "some sites block embedding → open live ↗" note, since many send `X-Frame-Options`). **#3 richer filtering + count:** added a "What you need" goal axis (Online store/Business site/Editable CMS) beside Platform, a live `aria-live` "X of 5 highlights shown" status, and an empty state. **#4 case-study links:** modal supports a per-card `data-case` "Read case study" button — **left empty for now** (case-study.html is the Lumen *template*; don't link real clients to it until per-project pages exist). **#5 honest context tags:** each card got an **industry** chip (Retail/Food & drink/Property/Software/Agency) — inferred, *confirm with client*. **#6 detail modal:** "View details" button + clickable screenshot open a lightbox (large shot, scope, tags, live link). **#7 goal chips:** see #3. **#8 shimmer/blur-up:** `.pj-skel` skeleton + image fades in on load (`.pj-shot.is-loaded`). **#9 stat band + trusted-by:** `.proj-stats` count-ups (18/3/6) + recent-client wordmarks. **#10 clickable chips → hash:** platform chips filter the grid and set `#work=<key>` (read on load). Filtering unified via per-card `data-tags`. Files: `index.html` (section + modal), `styles.css`, `main.js` (4 IIFEs: filter / count-ups / screenshots / modal). **Caveats to verify live:** mShots can return a "generating…" placeholder on first hit then fill on reload; the "6 industries" stat + industry chips are unconfirmed; live-preview blanks for sites that block framing. `node --check` passes — needs a real-browser pass.
- **2026-06-20 (pricing — packages + care plans redesign)** — **Packages:** added a per-type accent system (`.pc-business`/`.pc-ecom`/`.pc-app` set `--pc`/`--pcbg`) — tinted icon tile, scaleX top-accent bar on hover/featured, and replaced the awkward "Pricing on request" price slot with a clean "● Quoted to your scope" tag (`.pr-quote`, dot in the type color). **Care plans:** converted the white card grid into editorial **level rows** (`.care-rows`/`.crow`) — name + best-for + a signal-bar **support-level indicator** (`data-lvl` 1–4 fills pink bars), coverage, response time; Growth highlighted; the "Not sure which?" card became a dark full-width CTA banner (`.care-cta`). This matches the home maintenance row style and visually separates care from the package grid. All in `pricing.html`; no console errors. (A pink left-accent edge on add-on cards was tried then removed per feedback — add-ons use the plain uniform border.)
- **2026-06-20 (pricing — section motion + backgrounds)** — Added per-section scroll-in variety + ambient background motion (all in `pricing.html`, reduced-motion guarded). Reveal modifiers `.r-left/.r-right/.r-scale` + `.stagger` (nth-child delays) so each section enters differently (intros alternate sides, cards scale/stagger). Backgrounds/interactions: hero drifting color blobs (`.pr-hero::before/::after`); cursor-follow pink spotlight on the packages section (`.spot` + `--mx/--my` via inline JS); drifting quiz glow; count-up trust stats (`[data-count]` + IntersectionObserver); drifting "PRICING" watermark behind the FAQ (`.wm-sec`); hover lifts on how-it-works steps + add-ons + comparison rows. No new deps; no console errors.
- **2026-06-20 (pricing quiz — expanded 3→6 questions)** — The "Find your fit" quiz now asks 6 questions (type → size → key need → who-updates → what's-ready → care) with a smarter result: scope nudges Starter↔Business, "I sell products" forces Ecommerce, "bookings" forces Custom App; the result lists a **"Likely to include"** block (CMS, blog, Branding/Copywriting add-ons) so clients learn what their build involves. Progress bar generated from the question count. Intro copy updated to "6 quick questions". All in `pricing.html`. Verified, no console errors.
- **2026-06-20 (hero — performance + mobile)** — **#2 LCP/CLS:** the H1 (the LCP element) was split into letters and hidden (`autoAlpha`) immediately, gating first paint on JS. Now `main.js` (a) **returns early on `prefers-reduced-motion`** so the headline stays plain instantly-painted text, (b) wraps the split+reveal in `requestAnimationFrame` so the browser paints the real headline first then enhances, and (c) keeps **spaces as real text nodes** (was a span per char incl. spaces — a chain of inline-blocks never wraps → could overflow narrow screens). Also **deferred the particle canvas** start to `requestIdleCallback` (timeout 900, `setTimeout` fallback) so it doesn't compete with LCP. **#3 mobile:** added a `@media (max-width: 600px)` block turning the 3-up hero card deck (32% → ~110px-wide cards, unreadable) into a **scroll-snap swipe carousel** (cards `flex: 0 0 66%`, max 260px, snap-center, hidden scrollbar) and reduced the H1 clamp. Tablets (≤860px) keep the 3-up row. Card flips + bubbles stay hover-only; the slot pill + essentials live on the always-visible front and each card links through on tap.
- **2026-06-20 (pricing page — full rebuild, 10 features)** — Rebuilt `pricing.html` (uses `work.css` + page-inline `<style>`/`<script>`). Added: (1) **Build ↔ Care toggle** (tabs swap the website-packages vs care-plans panels); (2) **comparison table** of the 4 packages (Business column highlighted); (3) **"Quoted separately"** line on each package card; (4) **"Find your fit" quiz** — 3 questions → recommends a package (+ a care plan if they want ongoing support), with Get-my-quote CTA + retake (vanilla JS, no deps); (5) **"Most popular" ribbon** + icon on the Business card; (6) **"How pricing works"** 3-step strip; (7) **pricing FAQ** (native `<details>` accordion — "why no prices", "what affects cost", etc.); (8) **add-ons** grid (extra pages, copywriting, branding, photography, migration, integrations — all "quoted separately"); (9) **trust band** (stats + testimonial — PLACEHOLDER, flag in code); (10) polish — package icons, sticky mobile "Start your project" bar, quiz dark section w/ pink glow. Also: fixed the nav CTA "Free audit" → "Start your project" (site consistency), and **removed the hour-count chips** from care plans (now response-time + coverage, matching the home maintenance + the no-hours confidentiality rule). Still no peso prices anywhere. Verified live (toggle, quiz flow, FAQ, no console errors).
- **2026-06-20 (hero — slot pills + reveal-on-load fix)** — (1) Replaced the single hero "Now booking new projects" availability pill with a **per-card slot pill** (`.plat-slot` + pulsing green `.ps-dot`, on each card front above the name): Webflow "2 slots available", Shopify & WordPress "1 slot available". Dark-glass pill so the green dot reads on all three colored cards. **These are hand-maintained numbers — update as bookings fill.** (Removed `.hero-status`/`.hs-dot`; kept the `hsPulse` keyframe for the dot.) (2) **Fixed: hero foot was hidden until scroll.** The foot's `.reveal` elements (the "We build…" lead, CTA buttons, email) sit at the bottom of the 100vh hero — below the generic reveal's `top 80%` scroll-trigger — so they stayed at `opacity:0` on first load. `main.js` now plays `.hero .reveal` **on load** (staggered) and excludes `.hero` from the scroll-triggered generic reveal (`customSel`).
- **2026-06-20 (hero banner — 5 conversion/clarity adds)** — Added five things to the hero (it was visually rich but thin on trust/clarity above the fold): (1) **Availability pill** above the label — `.hero-status` with a pulsing green dot, "Now booking new projects" (evergreen, no date upkeep; SOP-safe — availability, not a guarantee). (2) **Proof strip** in the foot under the subtitle — `.hero-proof`: "18+ projects delivered" + the three platform mono-logos (simpleicons in ink `#111110` @ 0.4 opacity). Real count (18). (3) **Secondary CTA** — outlined-pill `.btn-ghost` "See our work →" → `portfolio.html`, next to the primary in a new `.hero-btns` row, so browsers who won't email yet have a path. (4) **Rotating service word** — `.hero-lead` "We build [Shopify stores / Webflow sites / WooCommerce shops / custom web apps] that convert" — pink `.rot-word`, JS rotator (`main.js`, 2.6s cycle, fade/slide swap). (5) **Value clarity** — the lead is the new what-we-do line (the H1 is just the brand). Foot restructured: left column (`.hero-foot-l` = subtitle + proof) vs right (`.hero-cta` = buttons + email). Mobile: foot stacks + left-aligns. Reduced-motion: dot pulse disabled. Files: `index.html`, `styles.css`, `main.js`. **Follow-up fixes (per screenshot feedback):** the "We build [rotator] that convert" lead was floating between the H1 and the cards and **overlapped the card tops** — moved it down to the foot's left column, **replacing** the old "Shopify revamps…" subtitle (left-aligned, sentence case so the pink rotating word reads). Also fixed: the proof strip was invisible (it sat below the `.reveal` `top 80%` scroll-trigger so it never animated from `opacity:0` → removed `reveal`, now always shown); and the two foot CTAs were vertically staggered (base `.btn { margin-top:56px }` outranked `.hero-btn` in the cascade → added `.hero-btns .btn { margin-top:0 }`).
- **2026-06-20 (hero cards — flip BACK redesign)** — Reworked the back face of the 3 hero platform cards (feedback: the flip shipped but the back read as "same card, new text"). Five changes: (1) **Distinct matte surface** — back is now a deep platform-tinted *spec sheet* (dark gradients in `styles.css`) instead of the bright front gradient, so the flip has a glossy-cover→matte-inside payoff. (2) **Glossy sheen removed from the back** — the animated diagonal `::after` sweep is now front-only (`.plat-front::after`); the back `::after` is a faint blueprint **grid** + accent corner glow behind the content. (3) **Bullets → icon rows** — `.plat-back-list` (yellow dots) replaced by `.plat-back-rows` (icon-tile + label), reusing each platform's front feature SVGs; accent-tinted via new `--accent-bright` vars. (4) **CTA is now a filled white pill button** (`.plat-back-cta`), not a text link. (5) **Honest proof anchor** in a new `.plat-back-head` row next to "Best for": Shopify "6 stores shipped" / Webflow "10 sites built" / WordPress "WooCommerce ready" (real portfolio counts — SOP-safe, no guarantees). Plus back content now **staggers in ~0.42–0.66s after the flip** (per-element opacity/translateY). Reduced-motion: stagger/slide disabled, back still cross-fades. Files: `index.html` (3 back faces), `styles.css`. Flip mechanics untouched. **Refinement pass:** shortened the fit sentences (they wrapped to ~3 lines and risked clipping the CTA on the 320px side cards, since `.plat-face` is `overflow:hidden`), gave `.plat-back` its own 12px gap (was doubling the inherited 20px gap + margins), trimmed the fit clamp to 16–20px, and added `aria-hidden` to the decorative row icons. **Known tradeoff (not fixed):** the flip is hover/focus only, so touch users never see the back — the front still carries the logo, desc, feature icons, "View work →" and the whole card links through, so nothing essential is back-only.
- **2026-06-20 (hero cards — flip shadow cleanup)** — Removed the awkward flat box-shadow from the flipping card wrapper/rotating `.plat-flip` layer. Added a single soft `.card::after` ground shadow under each hero card, and removed the old GSAP hover `boxShadow` tweens in `main.js`, so the 3D card can rotate without dragging a rectangular shadow through the flip.
- **2026-06-20 (hero cards — more physical 3D flip)** — Reworked the hover/focus flip so the actual card faces rotate instead of only the inner content. `.plat` is now a transparent 3D container, `.plat-front`/`.plat-back` carry the platform gradients, and `.plat-flip` owns the rotating card body with perspective, preserve-3d, dynamic shadow, a center edge sliver, face lighting, and lifted `translateZ()` content. This makes the flip read more like a real physical card turning.
- **2026-06-20 (hero cards — smooth flip backs)** — Added a smooth 3D hover/focus flip to each homepage hero platform card using a nested `.plat-flip` layer so it does not fight the existing GSAP float/tilt on `.polaroid`. The front face keeps the existing logo/star/icons/CTA. The back face now shows a compact "Best for" label, one fit sentence, three "What you get" bullets, and a platform-specific work CTA for Webflow, Shopify, and WordPress. Added reduced-motion fallback that fades the back instead of rotating.
- **2026-06-20 (hero cards — preview images removed)** — Removed the placeholder mini browser preview/image strips from all three homepage hero platform cards per visual feedback. The cards remain clickable and keep their platform logo, star badge, watermark, feature icons, "View work" affordance, sheen, float/tilt, and hover behavior. Deleted the now-unused `.plat-mock` / `.pm-bar` / `.plat-reveal` CSS and lightly adjusted card spacing/watermark opacity so the cards still feel intentional without the screenshot window.
- **2026-06-20 (hero cards — content/interaction)** — Added 5 things to the hero platform cards: (1) **mini browser mockup** per card (`.plat-mock` — title-bar + dots + placeholder picsum preview; swap for real screenshots later); (2) each card is now a **link** to its platform page (`.polaroid` is an `<a href>`, Webflow/Shopify/WordPress → respective pages) with a **"View work →"** affordance; (3) **feature-icon row** (`.plat-feats`/`.plat-feat`, 3 inline-SVG icons per card); (4) **hover-reveal** services panel over the mockup (`.plat-reveal`, shows on `.card:hover`); (5) **cursor-reactive sheen** (`.plat-sheen` + `--sx/--sy` set via a per-card `mousemove` in `main.js`). Replaced the old `.plat-chip`. Card markup in `index.html`, styles in `styles.css`, sheen JS in `main.js`. Bubbles still float; cards still hover-lift + parallax. Note: mockups + "5-star" badges are placeholder.
- **2026-06-20 (hero bubbles v4)** — Removed ALL card standouts (ribbon/flag/stamp) per feedback — `.card-tag`/`.tag-flag`/`.tag-stamp` CSS deleted, markup removed from all 3 polaroids. Cards now show only their floating narrative bubbles + the existing star badge. Shopify keeps 4 bubbles (Secure checkout, Conversion ↑, Sells 24/7, Fast checkout).
- **2026-06-20 (hero bubbles v3)** — Removed the Webflow ribbon (`.tag-ribbon` gone). Added 2 more bubbles to the Shopify card ("Sells 24/7", "Fast checkout") — now 4, cascading in two rows below the card. Standouts now: Shopify "Built to sell" flag + WordPress "KNB APPROVED" stamp (Webflow has none — just its bubbles).
- **2026-06-20 (hero bubbles v2)** — Per feedback, dropped the pink starburst lead (`.nb-burst` removed). Each card now has a **platform-colored standout** pinned at the card-top (rides the card's float): Webflow = white "Client favourite" **ribbon** (blue text), Shopify = white "Built to sell" **flag** (green text), WordPress = "KNB APPROVED" dashed **stamp** (teal) — classes `.card-tag` / `.tag-ribbon` / `.tag-flag` / `.tag-stamp` in `styles.css`, markup inside each `.polaroid`. Standouts use white/light fills with platform-colored text so they contrast the colored cards. Lead bubbles reverted to normal white bubbles. **Bubbles now float continuously at varied speed/amplitude** (`nbFloat` + per-`:nth-child` `--dur/--amp/--wob/--dly`), so none sit static; still hover-revealed, hidden <1200px, respects reduced-motion.
- **2026-06-20** — Hero narrative bubbles: implemented all 5 backlog items (see §5b). Collision-proof repositioning (no headline overlap, no edge clipping at 1280), `.nb-burst` spiky starburst lead bubble (pink, drop-shadow, speed lines), halftone texture + uneven borders inside bubbles, reading-order staggered reveal with alternating rotation, and benefit/proof/verb copy (≤3 words). 4 bubbles per side card, 3 on the center card. Reveal still hover-only; hide breakpoint raised 980→1200px. Files: `index.html` (bubble markup), `styles.css` (`.nb*`). Optional tap-to-reveal for touch still pending.
- **2026-06-20 (hero bubbles)** — Hero platform cards got **comic/manga speech bubbles** (3 per card, 9 total): thick black outline, hard offset shadow, bold text, pointed tail toward the card, springy pop-in + idle wobble. **Now reveal on HOVER only** — hidden by default; hovering a card pops in its 3 bubbles staggered (CSS-driven via `.card:hover .nb`, `:nth-child` transition-delays). Removed the old load-entrance/fact-cycle JS. Markup inline in `index.html` (inside each `.card pX`), styles `.nb`/`.nb-in`/`.tail-*` in `styles.css`. See the "🔭 NEXT FOCUS" section for tuning notes (count 3→5, positions, copy, optional burst/halftone).
- **2026-06-20 (later)** — Added **pricing.html** (client-facing package "Starting at" prices + 5 maintenance care-plan fees, scope, notes — uses real SOP client-facing pricing), **about.html**, **404.html**. **Nav improved site-wide**: added Pricing + About to nav-links and mobile menu across all pages with active states (via script). Populated **portfolio.html (18 cards, filterable)** and platform pages with the **18 real client projects** (Shopify ×6, Webflow ×10, WordPress ×2), each card links to its live site — *platforms were guessed from URLs; client should confirm/correct.* Rebuilt home **Projects** cards (browser frame, hover-pan, live link, tech chips, platform accent, bento, count-up; before/after dropped — no old-site access). Maintenance tier icons. *(There is also a `case-study.html` from a parallel session.)*

- **2026-06-20** — Maintenance section is now a **5-tier plan list** (`.maint-alt` / `.mrow`: Basic Care, Standard Support, Growth Support [featured], Premium Support, Custom System Support) — coverage/best-for only, no pricing. Added **tier icons** (shield / wrench / rocket / crown / gear; featured icon in pink). *(Note: platform pages shopify/webflow/wordpress, portfolio, contact, work.css/work.js were also enhanced in a parallel session — before/after comparison slider, project filter tabs, `.proj-scope` lines, `.cta-head` blocks, stat count-up. Re-read those files before editing.)*
- **2026-06-19** — Portfolio audit items 1–10 implemented. **(1) SEO:** added meta description + Open Graph + Twitter tags to `portfolio.html` (og:image/og:url left as TODO — need the live domain). **(2) Alt text:** all 18 grid images now have descriptive alt (name + platform); decorative spotlight icon stays empty. **(3) Perf:** `loading="lazy"` + `decoding="async"` on all 18 grid images. **(4) A11y:** filter buttons use `aria-pressed` + a `role=group`; an `aria-live` `#filterStatus` announces the visible count; the before/after handle has `aria-valuemin/max/now` updated live (drag + arrow keys). **(5)+(6) Marquee:** wrapped as `role="img"` with one honest label so screen readers don't read the duplicated row twice (`aria-hidden` on the track). **(9) Testimonials:** new section before the CTA — **PLACEHOLDER quotes with `[Client name]`/`[Sample Brand]` attributions; must be replaced with real, attributable quotes before launch.** **(10) Case studies:** new `case-study.html` reusable template (Lumen example — hero, meta row, before/after, Challenge/What we did/Result, gallery, next-project, CTA); the portfolio spotlight CTA now links to it ("Read the full case study →"). New CSS in `work.css` (`.sr-only`, `.testi-*`, `.cs-*`); new JS in `work.js` for the a11y wiring. **Still content-dependent (6/7/8):** marquee brand names, all picsum images, and the "18/3/12+" stats are placeholders — swap for real before launch.
- **2026-06-19** — Checked all site copy against KNB's internal **Package Coverage / Maintenance / Designer Payment guide** (`.docx`). Findings: the 4 **Plans** panels, the **Services** block, and the **contact** dropdown already matched the guide's Section 2 coverage (page counts, revision rounds, support windows, included/excluded splits) — no changes needed. No prices or guarantee/ranking claims anywhere (compliant). **Changed:** rebuilt the **Maintenance** section from 4 generic care themes into the guide's real model — 5 named tier cards (Basic Care → Custom System Support, Growth = "Most popular") with coverage + response time. Deliberately **no peso amounts and no consumable-hour counts** (confidential). New CSS `.maint-head`/`.maint-plans`/`.mplan` in `styles.css`; old `.maint-grid`/`.maint-item`/`.maint-list` styles now dead (safe to delete). NOTE: the guide still calls step 1 a "free website or store audit" — the site intentionally diverges (uses "free consultation" / "Start your project") per the prior CTA decision, to include new-build customers.
- **2026-06-19** — Site-wide CTA rewording. Replaced "Get a free audit" / "Free audit" with **"Start your project"** on every button + nav across `index.html`, `portfolio.html`, `shopify/webflow/wordpress.html`, `contact.html` (contact form submit → "Start my project"; CTA-box eyebrows → "Get started" to avoid duplicating the button). Reworded the *prose* "audit" mentions (hero, process step 01, plans-sub, why-point, FAQ, contact lead/step) to "free consultation" + goal-first language so new-build customers aren't excluded. Left intact: the internal `auditForm` id/JS in `contact.html`, and "Store audit & customer-journey review" in the Ecommerce plan (a genuine deliverable). `headline-preview.html` (scratch) not touched.
- **2026-06-19** — Full portfolio **hub** (`portfolio.html`) rebuild combining 5 ideas: (1) **filterable work grid** — all 18 projects on the hub with platform filter tabs (All/Shopify/Webflow/WordPress); (2) **editorial mosaic** — a wide "feature" tile (`.proj-card.feat` spans 2 cols) in the All view, auto-resets to uniform when a filter is active; (3) **proof-forward hero** — animated stat counters (18 projects / 3 platforms / 12+ industries) + a CSS client-logo marquee (placeholder wordmarks); (4) **case-study spotlight** — Lumen Apparel with a draggable before/after slider (grayscale→color) + Problem/Approach/Outcome (scope-only, no metrics per SOP); (5) **motion** — per-card platform accent bar + colored hover wash + scroll reveals. New CSS in `work.css` (hero stats, marquee, spotlight + `.ba` slider, filter tabs, `.pf-grid` mosaic) and new JS in `work.js` (count-up, filter tabs, before/after drag — all guarded so the platform pages are unaffected). Cards still link to platform pages (no per-project detail pages yet). Verified live (hero/spotlight/grid/filter) at desktop width. Note: counters use `requestAnimationFrame`, which is paused in hidden tabs — fine in a real visible browser. The old `.cats`/`.cat-card`/`.cats-grid`/`.plat-shopify|webflow|wordpress` styles in `work.css` are now **dead** (the hub no longer uses the 3-card menu) — safe to delete.
- **2026-06-19** — Revamped the shared CTA block (`.cta-box` in `work.css`, markup on `portfolio.html` + all 3 platform pages). Was a plain centered black box; now an asymmetric editorial split (headline left / pitch + action right) with a pink "Free audit" eyebrow, dotted texture, a pink corner glow, an arrow button, and a green-dot trust row (Free · No obligation · …). Collapses to a single column under 860px. Trust items are scope-only (no guarantees, per SOP). Verified at mobile + desktop widths via the preview.
- **2026-06-19** — Portfolio "structure + honesty" pass. Fixed false project counts on the hub (`portfolio.html`: Shopify 8→6, WordPress 7→6; all three grids actually hold 6). Removed the dead-end "View project →" tag from platform-page cards (no detail pages exist) and added an honest **scope line** per card (describes *what we did* — scope only, no invented metrics, per the no-guarantees SOP rule). Added a **"Featured work"** section to the hub (`portfolio.html`) — 3 cards (one per platform) that link to their platform pages, so the portfolio landing shows real work instead of being a bare 3-button menu. `work.css`: restructured `.proj-meta` (now `.proj-top` row + `.proj-scope`), added `.proj-grid.cols-3`, made `a.proj-card` the clickable variant. Added `.claude/launch.json` (static-server preview config; note: `python3 -m http.server` fails under the Claude preview sandbox but works in a normal terminal).
- **2026-06-19** — Created this handoff doc. Refactored `index.html` (inline CSS→`styles.css`, JS→`main.js`); fixed horizontal scroll (`overflow-x: clip`). Built interactive Plans selector + redesigned detail panel (chips + included/excluded). Revamped Projects (portfolio button, filter tabs, featured card, platform chips/links, outcome lines). Added floating tech-logo cluster to Services (bigger/compact). Process chevrons → full-width left→right "comet". Added sections: Process, Plans, Maintenance, Why KNB, FAQ. Per-section distinct scroll animations + per-section backgrounds. Hero platform cards + many hero animation iterations.
